[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "nextpytk"
version = "0.2.0"
description = "Flask-style decorator API for tkinter GUI: schema-driven, A11y-first, agent-compatible."
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }
authors = [{ name = "Takuya Nishimoto" }]
dependencies = []

[project.urls]
Homepage = "https://github.com/nishimotz/nextpytk"

[tool.hatch.build.targets.wheel]
packages = ["src/nextpytk"]

[tool.hatch.build.targets.sdist]
include = ["src/nextpytk", "README.md", "ROADMAP.md"]

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pyright>=1.1.409",
]
